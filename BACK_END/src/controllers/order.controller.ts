import { Request, Response } from 'express';

export const getMyOrders  = (_req: Request, res: Response) => { res.status(501).json({ message: 'Not implemented' }); };
export const checkout     = (_req: Request, res: Response) => { res.status(501).json({ message: 'Not implemented' }); };
export const getOrderById = (_req: Request, res: Response) => { res.status(501).json({ message: 'Not implemented' }); };